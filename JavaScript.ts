let baseSalary:number=3000;
let roomRent:number = 2500;
let groceries:number = 1500;

function calculateSalary(baseSalary:number,roomRent:number,groceries:number) {
    return baseSalary + roomRent + groceries;
    
}

// object orianted stype

const emplyeeSalar = {
    baseSalary:3000,
    roomRent:2500,
    groceries:1500,  
}